// import ManageTask from "./ManageTask.js";

export default class TemplatePlat {

    showDetailsPlat(task){

        // let eldetail = document.querySelector('[data-js-task-detail]');
        // let elTemplate = document.querySelector('[data-js-template-detail]');
        // let elCloneTemplate = elTemplate.cloneNode(true);

        // if (task.description == '') task.description = 'Aucune description disponible.';
        
        // for (const prop in task){
        //     let regExp = new RegExp(`{{${prop}}}`,'g');
        //     elCloneTemplate.innerHTML = elCloneTemplate.innerHTML.replace(regExp, task[prop]);
        // }

        // let newTask = document.importNode(elCloneTemplate.content, true);

        // // injection dans le dome de la nouvelle tache
        // eldetail.innerHTML = '';
        // eldetail.appendChild(newTask);     
    }
}
